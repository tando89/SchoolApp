<resources xmlns:android="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="MyTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">#FFF</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">#000</item>
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <!-- Customized ActionBar. Change its background to white color -->
    <style name="MyActionBar" parent="@style/Widget.AppCompat.Light.ActionBar">
        <item name="android:background">#FFF</item>
        <item name="android:titleTextStyle">@style/MyActionBarTitleText</item>
        <item name="titleTextStyle">@style/MyActionBarTitleText</item>
    </style>

    <!-- Customized text title on the ActionBar. Change to black color. -->
    <style name="MyActionBarTitleText" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">#000</item>
    </style>
    <!-- Customized preference activity -->
    <style name="PreferenceScreen"  parent="android:Theme.Material.Light">
        <item name="android:colorPrimary">#FFF</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">#000</item>
        <item name="android:textColorPrimary">#000</item>
    </style>
    <!-- Customized theme for other activities -->
    <style name="customTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:colorPrimary">#000</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">#000</item>
    </style>
</resources>
